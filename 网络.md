网络

# 1. HTTP和HTTPS的区别

答： HTTP是明文传输，HTTPS是密文传输比较安全
 HTTP的默认端口是80，HTTPS默认端口是433
 HTTP建立连接只需要TCP三次握手，HTTPS需要TCP三次连接之后还要建立SSL/TSL安全连接
 HTTPS需要获取CA权威安全认证，确认服务器可信

标准答案：HTTP 默认端口为 80，明文传输；HTTPS 是 HTTP over TLS，默认端口为 443，通过 TLS 提供加密、完整性校验和服务器身份认证。HTTPS 通常先完成 TCP 三次握手，再进行 TLS 握手；证书由 CA 信任链校验服务器身份。

# 2. TCP和UDP的区别

TCP：可靠、有序、面向连接、面向字节流，但慢、开销大。
UDP：不可靠、无序、无连接、面向数据报，但快、开销小、支持广播多播。

# 3. TCP 的三次握手
客户端                                服务端
CLOSED                                LISTEN
  |                                      |
  | ---- SYN, seq=x ------------------>  |
  |                                      | 进入 SYN_RCVD
  | <--- SYN+ACK, seq=y, ack=x+1 ------  |
  | 进入 ESTABLISHED                     |
  | ---- ACK, seq=x+1, ack=y+1 ------->  |
  |                                      | 进入 ESTABLISHED
  |                                      |
ESTABLISHED                           ESTABLISHED
## 第一次：客户端-->服务端
发送 SYN=1, seq=x
含义：我想建立连接，我的初始序列号是 x
客户端进入 SYN_SENT
## 第二次：服务端-->客户端
发送 SYN=1, ACK=1, seq=y, ack=x+1
含义：我同意连接，我的初始序列号是 y，我确认收到你的 x
服务端进入 SYN_RCVD
## 第三次：客户端-->服务端
发送 ACK=1, seq=x+1, ack=y+1
含义：我确认收到你的 y
客户端进入 ESTABLISHED
服务端收到后也进入 ESTABLISHED
## 注意：SYN 会消耗一个序列号，所以确认号是 x+1、y+1。

## 为什么进行三次握手：
1.确认双方的收发能力;
2.同步双方的初始序列号ISN也就是最后的x+1和y+1.

# 4. TCP四次挥手
客户端                                服务端
ESTABLISHED                           ESTABLISHED
  |                                      |
  | ---- FIN, seq=u ------------------>  |
  | 进入 FIN_WAIT_1                       | 进入 CLOSE_WAIT
  | <--- ACK, seq=v, ack=u+1 ----------  |
  | 进入 FIN_WAIT_2                       |
  |                                      | 应用程序处理完数据
  | <--- FIN, seq=w, ack=u+1 ----------  |
  |                                      | 进入 LAST_ACK
  | ---- ACK, seq=u+1, ack=w+1 ------->  |
  | 进入 TIME_WAIT                        | 收到 ACK 后进入 CLOSED
  |                                      |
  | 等待 2MSL                            |
  | 进入 CLOSED                           |
## 第一次：客户端 → 服务端
客户端发送 FIN=1, seq=u
含义：我没有数据要发了，想关闭连接。
客户端进入 FIN_WAIT_1。
注意：客户端不再发送数据，但仍可以接收数据。
## 第二次：服务端 → 客户端
服务端收到 FIN 后，回复 ACK=1, seq=v, ack=u+1
含义：我收到你的关闭请求了。
服务端进入 CLOSE_WAIT。
客户端收到 ACK 后进入 FIN_WAIT_2。
此时服务端可能还有数据没发完，可以继续发送。
## 第三次：服务端 → 客户端
服务端处理完数据后，发送 FIN=1, seq=w, ack=u+1
含义：我也没有数据要发了，可以关闭了。
服务端进入 LAST_ACK。
## 第四次：客户端 → 服务端
客户端收到 FIN 后，回复 ACK=1, seq=u+1, ack=w+1
含义：我确认收到你的关闭请求。
客户端进入 TIME_WAIT。
服务端收到 ACK 后进入 CLOSED。
客户端等待 2MSL 后进入 CLOSED。
## 注意：FIN 也会消耗一个序列号。

## 为什么进行四次挥手：
因为 TCP 是全双工的，每个方向必须单独关闭：
客户端发 FIN：表示客户端不再发送数据。
服务端回 ACK：表示服务端知道客户端要关了。
但服务端可能还有数据要发，不能立刻关。
等服务端数据发完，再发 FIN：表示服务端也不再发送数据。
客户端再回 ACK：确认服务端关闭。