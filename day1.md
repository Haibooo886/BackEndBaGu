## redis
# 1. redis有哪些数据类型
答: 基本数据类型 string,list,set,zset,hash,特殊数据类型 Bitmap(可用来存储0 1 数据比如签到表),haperloglog,GEO(地址信息),stream(消息队列)

标准答案：Redis 常用的基本数据类型有 String、List、Set、Hash、Sorted Set（ZSet）。此外还提供 Bitmap、HyperLogLog、GEO、Stream 等数据结构或数据类型。不同类型分别适合字符串/计数、列表、去重集合、对象字段、按分值排序、位图统计、基数估算、地理位置查询和消息流等场景。
# 2. Zset使用了什么数据结构
答： Zset底层使用的是 SkipList存的hash数据用来存 key-value分别对应 数据-score,利用hashmap的key不可重复特性来去重，通过score来排序。通常可用用做排行榜的使用。

标准答案：ZSet 底层同时使用哈希表和跳表。哈希表保存 member 到 score 的映射，便于快速查找 member 的分值；跳表按 score（score 相同再按 member）有序，便于范围查询、排名和顺序遍历。两者共同保证去重、快速定位和有序操作，常用于排行榜。
# 3. SkipList了解吗
答： SkipList其实就是多层双向链表的实现，最底层存储的是全部数据节点，越往高一层就会随机抽取部分节点来最为新一层的 全部数据节点，所以最后的结构也就是三角形。我们在查询的时候就会拿着 需要查询的 数据 去最高层比较如果大于此值就会跳到低一层继续比较以此类推。所以就可以大大简化查找时间。

标准答案：跳表是在有序链表上增加多级索引的概率型数据结构。最底层包含全部节点，上层按概率抽取部分节点；查找时从最高层向右移动，超过目标后下降一层，重复进行。平均查找、插入、删除复杂度为 O(log n)，最坏为 O(n)，平均空间复杂度为 O(n)。Redis 的跳表还支持前进和后退遍历。
# 4. 跳表的时间复杂度是多少？
答： 平均时间复杂度是O(log n), 但是也有可能极端情况最终到最底层链表查找时间复杂度O(n)。

标准答案：跳表查找、插入、删除的平均时间复杂度都是 O(log n)，最坏时间复杂度为 O(n)；空间复杂度平均为 O(n)。
# 5. 为什么MySQL不用SkipList？
答： 记不太的了，应该是数据量太大B+树查询较快

标准答案：MySQL 的数据主要存放在磁盘上，InnoDB 使用 B+Tree 更适合页式存储：树的扇出大、层数低，能减少磁盘 I/O；叶子节点按顺序连接，范围查询和排序效率高。跳表依赖大量指针和随机访问，节点分散，磁盘 I/O 和缓存局部性不如 B+Tree。
# 6. redis的使用场景
答: 缓存数据，分布式锁，消息队列。

标准答案：常见场景包括缓存、分布式锁、计数器和限流、Session/验证码等临时数据、排行榜、延迟队列、发布订阅以及 Stream 消息流。使用时要根据可靠性、持久化和一致性要求选择方案，不能默认把 Redis 当作唯一持久化数据库。
# 7. redis性能好的原因
答: redis是内存存储 查询速度相较于磁盘高出几个数量级。

标准答案：主要原因是以内存访问为主，避免磁盘 I/O；底层数据结构针对操作进行了优化；采用事件驱动和 I/O 多路复用处理连接，减少线程切换和锁竞争。Redis 6 以后网络 I/O 可使用多线程，但命令执行仍以单线程模型为主。
# 8. redis和MySQL如何保证一致性
答： 先更新数据库再删缓存。如果在删缓存过程中出现问题 没删除成功可以通过加一个合理的过期时间解决，也可以搭建redis主从集群来防止宕机引起的数据不一致问题。

标准答案：常用 Cache Aside 模式：读时先查缓存，未命中再查数据库并回填；写时先更新数据库，成功后删除缓存，而不是直接更新缓存。删除失败可通过重试、消息队列或订阅 binlog 异步补偿，并设置合理 TTL 兜底；高并发下可使用延迟双删、分布式锁或串行化更新。Redis 主从/集群解决可用性和故障转移，不能单独保证与 MySQL 的一致性。
# 9. 热KEY是什么，怎么解决
答：热KEY指的是被请求频繁访问的KEY导致压力大
解决：给热KEY做分片，多节点负载均衡。

标准答案：Redis中某个KEY被超高并发访问，流量集中落到Redis单个节点，造成该节点CPU，网卡压力过大，甚至阻塞。
解决方案：1. 热key分片：把一个key拆成多个key，goods_1,goods_2,访问时随机路由，分散到不同Redis节点
        2. 本地缓存：Caffeine等本地缓存，请求先读JVM本地缓存减少Redis访问
        3. 网关层缓存：在网关做缓存拦截
        4.避免大key：热key如果value过大也会放大压力
        5.写热key：不能单纯分片，可用队列消峰，合并写请求

## MySQL
# 1. 事务有哪些特性？
答： A(原子性)C(连续性)I(隔离性)D(持久性)

A(原子性)： 在事务中的多次数据库 增 删 改 操作必须同时成功或者同时失败。
C(连续性)： 
I(隔离性)： 每个事务之间的 操作相互隔离互不影响。
D(持久性)： 在事务提交之后数据将被持久化。


标准答案：ACID 分别是原子性（Atomicity，事务中的操作要么全部成功，要么全部回滚）、一致性（Consistency，事务执行前后数据库都满足约束）、隔离性（Isolation，并发事务相互隔离，程度由隔离级别决定）、持久（Durability，提交后的修改即使故障也不会丢失，通常由 redo log 等机制保证）。

# 2. 隔离性有哪些隔离级别？
答： 1. 读未提交 ：指的是一个事务操作的一个数据还未提交就可以被其他事务读取 会出现 脏读，
        不可重复读，幻读 等问题。
    2. 读已提交 ：指的是一个事务只可以读取其他事务提交后的数据 会出现 不可重复读，幻读
    3. 可重复读 ：指的什么忘记了 会出现 幻读。但是InnoDB后续通过MVCC和间隙锁解决了部分幻读
    4. 序列化 ：无问题。但是锁加得太大，影响性能。

标准答案：四种隔离级别是：读未提交（可能脏读、不可重复读、幻读）；读已提交（避免脏读，但可能不可重复读和幻读）；可重复读（同一事务重复读取同一行一致，标准 SQL 仍可能幻读，InnoDB 常通过 MVCC 和 next-key lock 抑制）；串行化（事务串行执行，并发度最低、开销最大）。
# 3. MySQL默认用的哪个级别？
答： MySQL默认用的 可重复读RR 

标准答案：MySQL InnoDB 默认隔离级别是可重复读（Repeatable Read，RR）。
# 4. 间隙锁的原理
答：原理应该是syncronized 

标准答案：间隙锁是 InnoDB 在索引记录之间的间隙上加的锁，阻止其他事务在该范围内插入记录，从而防止幻读。它只作用于索引范围，通常在 RR 下由锁定读或范围 UPDATE/DELETE 触发，并可与记录锁组成 next-key lock；与 Java 的 synchronized 无关。
# 5. 什么时候会加间隙锁
答： 若主键id 有 1,3,4,7,9 我们进行SELECT * FROM USER WHERE ID=2 FOR UPDATE;则会触发间隙锁将 id(1,3)这个范围锁住防止数据发生变化。

标准答案：在 InnoDB 的 RR 隔离级别下，对索引执行范围锁定读或范围 UPDATE/DELETE 时可能加 next-key lock，其中包含间隙锁。主键值为 1、3、4、7、9 时，唯一索引上的 `WHERE id = 2 FOR UPDATE` 若 2 不存在，通常会锁住其所在间隙 (1,3)，阻止插入 id=2。实际锁范围取决于索引、执行计划、隔离级别和是否命中唯一索引。
# 6. undolog撤销过程具体是怎么撤销的
答： undolog的实现就是在事务中进行 1.增加的时候 记录主键值，若需要撤销直接根据主键值删除
     2.删除的时候 记录删除的数据的所有信息，需要撤销则插入回去
     3.修改的时候 记录修改前的所有数据，需要撤销则修改回去

标准答案：Undo Log 记录事务修改前的版本，用于事务回滚和 MVCC 历史版本读取。插入时记录对应的删除操作；删除时保留被删除行的旧值，回滚时恢复；更新时记录更新前的列值，回滚时恢复旧值。Undo Log 通过指针组成版本链，事务提交后由 purge 在无事务需要这些版本时清理。
# 7. 怎么决定建立哪些索引
答： 索引的存在本质是为了加快数据库的查找的。
    所以如果此字段会被多次用在SELECT ,ORDER BY, GROUP BY后面则可以添加为索引
    若为多个字段联查也可以添加联合索引比如(id,name,age)

    索引不适合创建在：比如gender这种数据单一通过索引回表的速率 还不如查全表，此时就算添加了索引，数据库的择优器也会自动选择查全表。 

标准答案：根据真实查询和写入负载决定索引：优先为高频且选择性较高的 `WHERE`、`JOIN`、`ORDER BY`、`GROUP BY` 条件建立索引；联合索引按查询条件、排序和选择性设计，并遵循最左匹配原则；尽量使用覆盖索引减少回表。索引会占空间并增加写入成本，低选择性字段单独建索引通常收益有限，最终应结合 `EXPLAIN` 和实际耗时验证。
# 8. 最左匹配是什么，举个例子？
答： 最左匹配原则则是在建立联合索引的时候(a,b,c)查询条件必须从左往右存在
比如WHERE a=1,b=2,c=3 索引生效
    WHERE b=3,c=4 索引失效
    WHERE c=5 索引失效
因为在存储中联合索引是相对前面条件有序的 ，如果前面的条件不成立那么就是无序的

标准答案：联合索引 `(a,b,c)` 按 a、再按 b、再按 c 排序。使用 a，或 a、b，或 a、b、c 时可以利用左侧连续前缀；只使用 b、c 通常不能用于定位索引起点。出现范围条件后，后续列通常不能继续缩小扫描范围，但仍可能用于过滤或排序，具体以执行计划为准。
# 9. MySQL的存储引擎有哪些？为什么常用InnoDB？
答： InnoDB，MyISAM，memory
因为InnoDB提供完善的事务管理，保证数据的一致性，完整性

标准答案：
InnoDB，MYISAM,Memory，Archive
使用InnoDB的原因：1.支持ACID事务，支持回滚，提交 2.支持行级锁+间隙锁，并发性能好，MYSIAM只有表锁，并发差 3.支持聚簇索引 4.支持外键约束 5.崩溃安全恢复，依靠redo log，undo log，宕机不会丢数据 6.MYISAM不支持事务，表锁，适合只读静态资源
注：MYISAM优势：查询速度块，占用磁盘小，但是不支持事务，锁细粒度太大。
# 10. B+树和B树的比较
答： 1. B+树在非叶子节点只存储索引，在叶子节点才存储记录，B树是在所有节点都存储索引加记录
    2. 因为1的特点使得B+树和B树在存储相同的数据量时，B+树更加矮胖查询时I/O次数更少
    3.在范围查询的时候，B+树叶子节点是链表连接所以可以直接遍历查询，B+树要从根节点开始中序查询然后又回溯再查另一个树，效率极低。
标准答案：
    1. 数据存储位置：B树叶子，非叶子节点都存放完整数据行;B+树非叶子节点只存索引键，不存真实数据，完整数据全部放在叶子节点。
    2.磁盘IO：相同页大小，B+树非叶子节点能存放更多索引，树高度更低，磁盘IO次数更少。
    3.范围查询：B+树叶子节点用双向链表串联，范围查询直接遍历链表，效率很高;B树范围查询需要来回遍历树节点，不断回溯，性能差。
    4.等值查询：B树找到键就直接返回;B+树不管命中非叶子，最终都要走到叶子节点;但整体IO次数更低
    5.全表扫描：B+树直接遍历叶子链表即可;B树需要整棵树遍历。

注：MYSQL索引选择B+树最核心亮点：IO少，范围查询友好
# 11. 除了聚簇索引，还有什么索引？
答： 二级索引，唯一索引，联合索引

标准答案：聚簇索引之外统称二级索引（非聚簇索引）
        二级索引又分为：普通二级索引，唯一索引，联合索引（复合索引），前缀索引。
# 12. 二级索引存放的有哪些数据？
答： 二级索引存放的只有主键

标准答案：二级索引叶子节点存储：索引列的值+主键值
举例：建立索引index_name(name)，叶子存name的值+主键id
-- 查询的时候，如果查询字段不在索引里，就拿主键回表，去聚簇索引拿完整行数据，这个叫回表
-- ！不是只存主键，索引字段的值也存
# 13. 索引失效的情况
答： 1. 模糊查询like %值% 会导致索引失效
    2. 索引被函数包裹，会使索引失效
    3. 索引>数字，会导致索引发生隐式类型转换成数字，导致索引失效
    4. 联合索引必须满足最左匹配原则否则索引也会失效

标准答案：
    1. like以%开头：like '%abc'/'%abc%'索引失效，like 'abc%'可以走索引
    2. 索引列参与运算，函数操作：where data(create_time)='xxx';不能对索引列做函数。
    3. 隐式类型转换：字符串字段用数字去比较，索引失效。例：where str_colunm=123。
    4. 违反联合索引最左匹配原则，跳过左边索引列
    5. 使用or，一边列无索引，会造成整体索引失效。例：SELECT * FROM employee WHERE dept_id = 10 OR name = '张三';若dept_id或者name有一方无索引则索引失效
# 14. 事务隔离级别有哪些？
答： 1.读未提交：事务还未提交更改就能被其他事务读取
    2. 读已提交：事务更改提交之后才能可见
    3. 可重复读：事务读取的一行记录在整个事务中都不会变
    4. 串行化：用锁锁住整个事务区域，其他请求必须等待

标准答案： 1.读未提交：可以读到别的事务未提交的数据
        2. 读已提交：只能读到其他事务已经提交的数据
        3. 可重复读：同一事务内，多次读同一行，结果保持一致
        4. 串行化：最高级别，全部加共享锁，事务串行执行
# 15. 什么情况下会出现幻读
答： 读未提交，读已提交，可重复读都会出现幻读，后面可重复读通过MVCC和间隙锁解决了幻读

标准答案：幻读：同一个事务内，当前读多次查询，返回结果行数不一样，别的事务插入/删除了新数据
        读未提交，读已提交，可重复读都会出现幻读，后面可重复读通过MVCC+间隙锁解决幻读
# 16. 事务的MVCC是怎样实现的？
答：多版本并发控制

标准答案：MVCC：多版本并发控制，实现不加锁完成读操作，提升并发。
InnoDB MVCC核心实现：1.undolog回滚日志：保存数据修改前旧版本数据，用来生成历史快照版本链;
                    2.行隐藏字段：每行隐藏DB_TRX_ID（最后修改事务ID），DB_ROLL_PTR（指向undo log旧版本）;多个旧版本串联成版本链
                    3.read view读视图：事务开启时生成，里面保存活跃事务ID数组。
-- 查询的时候，拿read view规则去版本链过滤，判断哪些版本数据对当前事务可见，返回可见版本，实现快照读。
--RC隔离级别每次select生成新read view;RR隔离级别事务第一次select生成read view之后复用
# 17. 事务之间怎么避免脏读
答： 将事务隔离级别提升至读已提交及以上

## Java
# 1. Java线程里面有哪些状态 ？
答： unrunning 初始化线程完成但是未启动
    running 拿到CPU运行态
    wait 根据调度算法 CPU被调走
         被销毁状态

标准答案：Java `Thread.State` 有六种：NEW、RUNNABLE、BLOCKED、WAITING、TIMED_WAITING、TERMINATED。RUNNABLE 表示可运行（可能正在运行或等待 CPU）；BLOCKED 表示等待 synchronized 监视器锁；WAITING/TIMED_WAITING 分别表示无限期/限时等待。Java 没有单独的 RUNNING 枚举状态。
# 2. wait状态下的线程如何进行恢复到runing状态 ？ 
答： 被其他线程notify(ThreadId)唤醒，或者被notifyALL唤醒，或者其他任务完成拿到cpu

标准答案：WAITING 线程被同一监视器上的 `notify()` 或 `notifyAll()` 唤醒后，还要重新获得监视器锁，才能从 `wait()` 返回并进入 RUNNABLE；`notify()` 不接收线程 ID，也不保证立即获得 CPU。
# 3. notify和notifyAll的区别？
答： notify(ThreadId)需要指定唤醒的线程id唤醒对应的一个线程拿到CPU运行
    notifyAll则是唤醒所有wait的线程，所有线程竞争CPU

标准答案：`notify()` 唤醒该对象监视器上等待的一个线程，`notifyAll()` 唤醒全部等待线程；二者都必须在持有该对象监视器时调用，唤醒后仍需竞争锁。
# 4. 如何停止一个线程的运行？
答：  

标准答案：采用协作式停止：设置 `volatile` 取消标志并定期检查，或用 `Future.cancel(true)` 发送中断请求；阻塞方法捕获 `InterruptedException` 后恢复中断标志并退出。不要使用已废弃的 `Thread.stop()`。
# 5. 调用interrupt是如何让线程抛出异常的？
答：  

标准答案：`interrupt()` 只是设置中断标志。线程在 `sleep()`、`wait()`、`join()` 等可中断阻塞方法中时会抛出 `InterruptedException`；未阻塞时需通过 `isInterrupted()` 或 `Thread.interrupted()` 主动检查。
# 6. 如果是靠变量来停止线程，缺点是什么？ 
答： 

标准答案：停止标志若没有 `volatile` 或同步保障，可能不可见；线程若长期阻塞在不可中断的 I/O 或锁等待中，也无法及时检查标志。因此通常使用 `volatile` 标志配合中断机制。
# 7. volatile保证原子性吗？
答： volatile保证可见性，不保证原子性，不可重排序
可见性： 通过volatile修饰的变量，在发生变更后会立即刷星到主内存，而其他线程需要读取此变量也必须到主内存读不会在旧的缓存读取保证了可见性。
不保证原子性： 在非原子操作的时候会出现Bing发问题。
不可重排序： 重排序指的是程序在运行的时候会被优化器改变代码的运行顺序来提效，有时候会出现并发问题，比如单例模式的双重检查锁就是通过volatile的不可重排序实现。
# volatile标准答案：`volatile` 保证可见性和特定的有序性，但不保证复合操作的原子性，例如 `count++` 仍可能丢失更新。计数应使用原子类或锁；双重检查锁中的 `volatile` 用于防止对象发布重排序。

# 8. 那我们如何保证原子性？
答： 在非原子操作中可以添加syncronized可重入锁

标准答案：可使用 `synchronized`、`ReentrantLock` 等互斥锁，或使用 `AtomicInteger` 等原子类的 CAS 操作保护复合操作。
# 9. syncronized支持重入吗？ 如何实现的？
答： 支持可重入
    实现：底层是依靠操作系统的mutex lock实现每一个锁都有对应的threadid和status
    在获取锁的时候 判断threadid是否有值也就是是否有线程占领这个锁，如果没有就拿到锁并且更新threadid为自己的threadid，如果有锁占判断是否为自己的threadid如果是则为重入status+1.
    在释放锁的时候，若不是重入则直接释放
    若是重入则status-1，直到为0释放

标准答案：`synchronized` 支持可重入。同一线程再次进入已持有的监视器时增加重入次数，退出时递减，归零后才释放；JVM 通过对象头和监视器等机制实现，异常退出也会自动释放锁。
# 10. 编译型语言和解释型语言的区别
答：编译型语言：编译型语言就是运行之前先统一编译成字节码或者机器码，运行时再统一运行，跨平台能力差，例如：C/C++
    解释型语言：解释型语言是在运行时候由解释器逐行解释运行，效率较低但是跨平台能力好。例如：Python，JS

标准答案：1. 编译型语言：程序运行前，编译器把全部源代码一次性编译成机器码，生成可执行文件，直接在CPU运行。
-- 优点：执行速度快;
-- 缺点：编译产物和CPU架构绑定，跨平台差
-- 例子：C，C++，GO
        2. 解释型语言：没有提前完整编译，运行时解释器啊一边读源码一边翻译执行。
-- 优点：跨平台好
-- 缺点：执行效率偏低
-- 例子：PYTHON,JS,PHP

注意：Java属于半编译半解释：源码编译成字节码，字节码不直接跑CPU，交给JVM解释/即时编译执行，实现一次编译到处执行。
关键区分：编译型：源码->>机器码;Java：源码->>字节码;解释型：源码实时翻译
# 11. 动态数组的实现有哪些？
答： Vector和Arraylist

标准答案：1. ArrayList：最常用，非线程安全
        2. Vector：线程安全，方法加synchronized，性能差
        3. CopyOnWriteArraryList：并发场景动态数组，写时复制
底层原理：内部维护一个Object[]数组，对外提供可变长度API，容量不足自动扩容拷贝数组。
# 12. Arraylist和Vector的比较
答：Vector是线程安全的，Arraylist是非线程安全的
    Vector扩容是扩容0.5倍，Arraylist是扩容1倍

参考答案：1. 线程安全：ArrayList：方法不加锁，非线程安全，并发修改会出现并发修改异常;
                     Vector：大部分方法加synchronized，线程安全，并发性能差
         2. 扩容机制：ArrayList：默认扩容为原来1.5倍
                     Vector：默认扩容为原来2倍;支持自定义扩容增量
         3. 历史与使用：Vector是JDK早期集合，性能差，现在开发基本不使用;ArrayList是日常首选
# 13. HashMap的扩容条件是什么？
答：数组空间达到阈值，并且添加的此次元素发生hash碰撞

标准答案：
JDK1.8HashMap扩容触发条件：
1. map中元素总数量size>=threshold（阈值=当前数组容量x负载因子，默认负载因子0.75）;
2. 当前数组容量还没有达到最大容量 MAXIMUM_CAPACITY。

-- 注意：扩容和hash碰撞无关。
-- 树化条件：链表>8，并且数组容量>=64，链表转为红黑树;数组容量小于64，优先扩容，不树化。

## 网络
# 1. HTTP和HTTPS的区别
答： HTTP是明文传输，HTTPS是密文传输比较安全
    HTTP的默认端口是80，HTTPS默认端口是433
    HTTP建立连接只需要TCP三次握手，HTTPS需要TCP三次连接之后还要建立SSL/TSL安全连接
    HTTPS需要获取CA权威安全认证，确认服务器可信

标准答案：HTTP 默认端口为 80，明文传输；HTTPS 是 HTTP over TLS，默认端口为 443，通过 TLS 提供加密、完整性校验和服务器身份认证。HTTPS 通常先完成 TCP 三次握手，再进行 TLS 握手；证书由 CA 信任链校验服务器身份。

## 操作系统
# 1. 有哪些调度算法？
答： 先来先服务
    短任务优先
    优先级高优先
    时间片轮转
    还有记不得了

标准答案：常见 CPU 调度算法包括先来先服务（FCFS）、短作业优先（SJF）、最短剩余时间优先（SRTF）、优先级调度、时间片轮转、多级队列和多级反馈队列。优先级调度可能导致饥饿，可用 aging 缓解；时间片轮转适合分时系统。

# 2. 进程和线程的区别
答： 1.进程是操作系统资源分配的最小单位，线程是CPU调度和执行的最小单位
    2.每一个进程都有自己独立的数据空间，线程除了CPU没有其他独立的内存
    3.存在于同一个进程的线程共享数据空间
    4.一个进程的崩溃不会影响其他进程的执行，因为进程之间是相互隔离的，但是一个线程的崩溃会引起整个进程的崩溃

标准答案：
资源角度：进程是操作系统资源分配的最小单位；线程是 CPU 调度执行的最小单位。
内存空间：进程拥有独立完整虚拟地址空间； 同一进程内的线程共享进程资源（堆、方法区、文件句柄等）；线程自身拥有私有资源：线程栈、程序计数器、局部变量。
隔离性：进程之间相互隔离；一个进程崩溃，不会影响别的进程；同一个进程中任意一个线程崩溃，会导致整个进程直接崩溃。
开销：进程创建销毁、上下文切换开销大；线程轻量，切换开销小。

# 3. 为什么进程崩溃不会对其他进程产生很大影响
答： 进程隔离性：进程独有自己的内存空间，在一个进程崩溃之后，他的内存空间会被操作系统回收并不会影响到其他运行的进程
    进程独立性：进程运行时是相互独立的并没有数据或者文件传输，所以一个进程崩溃不会影响到其他运行的进程

标准答案：1. 每个进程拥有一套独立的虚拟地址空间，操作系统做内存地址映射，进程不能直接访问，修改其他进行的内存
        2. 当进行崩溃，操作系统会回收该进程占用的内存，文件句柄等全部资源;故障被限制在该进程内部
        3. 进程之间即便可以通过IPC通信交互数据，但是无法互相侵入对方内存，所以一个进程崩溃不会破坏其余进程进行
-- 补充：线程没有内存隔离，一个线程出现异常，整个进程直接挂掉
